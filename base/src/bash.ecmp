[info]
name = bash
version = 5.2.9
type = src
url = http://ftp.gnu.org/gnu/bash/bash-5.2.9.tar.gz 

[download]
curl $URL | tar -xz

[install]
./configure --prefix=/usr
make
make DESTDIR=$BUILD_ROOT install
