[info]
name = test
version = 1.0.0
license = MIT
url = x.com

[download]
mkdir $NAME-$VERSION
curl $URL > $NAME-$VERSION/download.txt

[install]
echo "INSTALLING $NAME-$VERSION" > install.txt
cp -r * $BUILD_ROOT

